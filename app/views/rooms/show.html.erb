<div class="card my-2 room-card">
  <div class="row h-100">
    <div class="col-4 d-none d-md-none d-lg-block border-end px-0 overflow-auto">
      <%= render @rooms %>
    </div>

    <div class="col-lg-8 px-0 d-flex flex-column">
      <div class="card-body border-bottom">
        <div class="row">
          <div class="col-1">
            <img src="https://placehold.jp/300x300.png" alt="avatar" class="img-fluid">
          </div>
          <div class="col-8 align-self-center">
            <p class="mb-0">Room<%= @room.id %>（<%= @room.user_ids.join(', ') %>）</p>
          </div>
        </div>
      </div>

      <div class="container py-1 overflow-auto h-100 position-relative">
        <div class="position-absolute" id="message-container" style="width: 95%;">
          <%= render @messages %>
        </div>
      </div>

      <div class="card-body border-top">
        <%= form_with model: [@room, @message], local: false do |f| %>
          <div class="row">
            <div class="col-10">
              <%= f.text_area :content, rows: '1', maxlength: '500', required: true, class: 'form-control msg-footer__textarea js-resize-textarea' %>
            </div>

            <div class="col-2 align-self-center">
              <%= f.submit '送信', disabled: true, class: 'btn btn-success', id: 'create-message-button' %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
